/**
 * firebase.js (Deprecated)
 * 
 * Firebase Admin SDK is no longer used.
 * Authentication has been migrated to Express JWT + PostgreSQL.
 * See src/middleware/auth.js and src/controllers/auth.controller.js.
 */

export const isFirebaseInitialized = false;
export default null;
